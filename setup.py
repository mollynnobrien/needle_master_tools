#!/usr/bin/env python

from setuptools import setup

setup(name='needlemaster',
      version='0.1',
      packages=['needlemaster'],
      install_requires=[
          'numpy',
          'matplotlib',
          'shapely',
      ],
     )
